# --- Database ---
# Dev (local SQLite, zero config):
DATABASE_URL="file:./prisma/dev.db"
# Prod (Postgres on Neon/Supabase/Railway), example:
# DATABASE_URL="postgresql://user:password@host:5432/kadoa?sslmode=require"

# --- App ---
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_WHATSAPP_NUMBER="41767627946"
NEXT_PUBLIC_BUSINESS_EMAIL="info@kadoa.ch"

# --- NextAuth (admin login) ---
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="change-me-in-production"
NEXTAUTH_URL="http://localhost:3000"
ADMIN_EMAIL="admin@kadoa.ch"
ADMIN_PASSWORD="change-me-strong-password"

# --- Stripe (TWINT is a payment method on Stripe CH accounts) ---
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Email (Infomaniak SMTP) ---
SMTP_HOST="mail.infomaniak.com"
SMTP_PORT="465"
SMTP_SECURE="true"
SMTP_USER="info@kadoa.ch"
SMTP_PASSWORD="your-mailbox-password"
SMTP_FROM="Kadoa Sourires & Souvenirs <info@kadoa.ch>"
